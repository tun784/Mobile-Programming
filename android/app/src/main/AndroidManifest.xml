<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Quyền truy cập bộ nhớ -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- Cấu hình ứng dụng -->
    <application
        android:label="flutter_classroom_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- Cấu hình Activity chính -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Theme cho Flutter UI -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <!-- Intent filter để khởi chạy ứng dụng -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Meta-data cho Flutter embedding -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- Meta-data cho App Check debug token -->
        <meta-data
            android:name="com.google.firebase.appcheck.debug_token"
            android:value="your-debug-token"/>
    </application>

    <!-- Queries để hỗ trợ xử lý văn bản -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>